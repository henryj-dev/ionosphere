{
  "name": "@ionosphere/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@ionosphere/core": "*"
  },
  "//": "pg·mysql2는 optional peer다 — SQLite(런타임 빌트인)만 쓰는 사용자가 안 받도록. src/{postgres,mysql}.ts는 동적 import로만 닿고, lint의 lazy-db-drivers가 정적 import를 막는다. bun·npm 둘 다 설치를 건너뛰는 것을 실측했다.",
  "peerDependencies": {
    "mysql2": "^3",
    "pg": "^8"
  },
  "peerDependenciesMeta": {
    "mysql2": {
      "optional": true
    },
    "pg": {
      "optional": true
    }
  },
  "devDependencies": {
    "@ionosphere/testkit": "*"
  }
}
